%h1 Locations

=link_to 'New Location', new_location_url

%ul
    -@locations.each do |loc|
        %li
            = loc.name
            = link_to "show", loc
            = link_to "edit", edit_location_path(loc)