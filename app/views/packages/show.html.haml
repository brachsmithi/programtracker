%h1 Package

.package
  = @package.name
  .discs
    - @package.disc_packages.each do |dp|
      .disc
        = dp.sequence
        .programs
          - dp.disc.programs.each do |p|
            = p.name

.links
  =link_to "Packages", packages_url