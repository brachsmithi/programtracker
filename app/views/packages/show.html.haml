%h1 Package

.package
  = @package.name
  .discs
    - @package.discs.each do |d|
      .disc
        .programs
          - d.programs.each do |p|
            = p.name

.links
  =link_to "Packages", packages_url