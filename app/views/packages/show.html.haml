%h1 Package

.package
  = @package.name
  .discs
    - @package.disc_packages.each do |dp|
      .disc
        = dp.sequence
        = dp.disc.format
        .programs
          - dp.disc.programs.each do |p|
            = capsule_program p

.links
  =link_to "Packages", packages_url