%h1 New Package

.package
  = form_with model: @package, id: 'form' do |f|
    .package-fields
      .form-group
        = f.text_field :name, autofocus: true
        = f.label :name
    .controls
      = link_to 'Create', "", {onclick: "$('#form').submit()", class: 'btn btn-primary'}
      = link_to "Cancel", packages_url, {class: 'btn btn-secondary'}

- if @package.errors.any?
  #error_explanation
    %h2
      = pluralize(@package.errors.count, "error") 
      prohibited this series from being saved:
 
    %ul
      - @package.errors.full_messages.each do |msg|
        %li= msg