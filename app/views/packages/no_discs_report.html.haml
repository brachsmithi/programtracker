%h1 Packages With No Discs

.top-controls
  = link_to 'Package List', packages_url, {class: 'btn btn-primary'}

#paginated-list
  = will_paginate @packages

  .index-entries
    - @packages.each do |pkg|
      .index-entry
        .index-entry-details
          = link_to pkg.name, package_path(pkg), title: 'show'

  = will_paginate @packages

#search-results