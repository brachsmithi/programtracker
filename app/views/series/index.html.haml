%h1 Series Index

.top-controls
  = link_to 'New Series', new_series_url, {class: 'btn btn-primary'}
  = form_tag(series_index_path, method: :get, remote: true) do
    = text_field_tag :search, params[:search], placeholder: 'Enter search term'

#paginated-list
  = render partial: 'paginated_series', locals: {series: @series}

#search-results