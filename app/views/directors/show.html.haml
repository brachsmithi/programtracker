%h1 Director

.director
  = @director.name
  - @director.director_aliases.each do |da|
    .alias
      = da.name
  - @director.programs.each do |p|
    .program
      = p.name

.links
  =link_to "Directors", directors_url