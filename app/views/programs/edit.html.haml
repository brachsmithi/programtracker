%h1 Edit Program

= form_with model: @program, url: program_path(@program.id), method: "patch" do |f|
  = f.label :name
  = f.text_field :name
  = f.label :sort_name
  = f.text_field :sort_name
  = f.label :year
  = f.text_field :year
  = f.label :director_ids
  = f.select :director_ids, @directors.collect { |dir| [dir.name, dir.id] }, {}, multiple: true
  = f.label :series_ids
  = f.select :series_ids, @series.collect { |ser| [ser.name, ser.id] }, {}, multiple: true
  = f.submit "Edit"

- if @program.errors.any?
  #error_explanation
    %h2
      = pluralize(@program.errors.count, "error") 
      prohibited this program from being saved:
 
    %ul
      - @program.errors.full_messages.each do |msg|
        %li= msg