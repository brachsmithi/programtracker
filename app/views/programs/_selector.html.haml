.modal-header
  %h5#myModalLabel.modal-title
    Select Program
  %button.close{"aria-label" => "Close", "data-dismiss" => "modal", type: "button"}
    %span{"aria-hidden" => "true"} ×
.modal-body
  .programs
    = select_tag 'selected_id_value', options_from_collection_for_select(@programs, :id, lambda { |prog| program_select_name prog }), {include_blank: ''}
.modal-footer
  %button.btn.btn-primary{ 'data-dismiss': 'modal', type: 'buttom', 'aria-hidden': 'true', onclick: "$('##{@set_id}').val($('#selected_id_value').val());$('##{@link_id}').html($('#selected_id_value option:selected').text());" }
    Set Program
  %button.btn.btn-secondary{ 'data-dismiss': 'modal', type: 'buttom', 'aria-hidden': 'true' }
    Close
